# Unknowns

1. **Supabase Schema & Migrations:** SQL definitions, triggers, and Row Level Security (RLS) policies are absent from the repository.
2. **Environment Variables:** Production variables (`VITE_SUPABASE_URL`, etc.) are external to the codebase.
3. **Deployment Strategy:** It is unknown where the frontend is deployed (Vercel, Netlify, Cloudflare, etc.).
